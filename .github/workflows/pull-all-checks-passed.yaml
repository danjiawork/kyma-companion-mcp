name: All Checks Passed

permissions:
  contents: read

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - edited
      - labeled
      - milestoned

jobs:
  all-checks:
    runs-on: ubuntu-latest
    permissions:
      checks: read
      contents: read
    steps:
      - name: Check if all checks passed
        uses: wechuli/allcheckspassed@984b54ac3cf7fc24c737f125e9b060cd265479db # v2.3.0
        with:
          delay: "1"
          retries: "10"
          polling_interval: "1"
